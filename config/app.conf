# Number of CPU cores to use 
[cpu_cores]
cores = 2

# Database settings (Mongo DB)
[db_settings]
db = malware 
collection = samples 

# Data locations (use full paths!)
[data_location]
yara_rules = /opt/MalZoo/data/yara_rules/
log_location = /opt/MalZoo/log/
repository_dir = /opt/MalZoo/storage/
