# Number of CPU cores to use 
[cpu_cores]
cores = 8

# Database settings (Mongo DB)
[db_settings]
db = malware 
collection = samples 

# Data locations 
[data_location]
yara_rules = data/yara_rules/
log_location = log/
repository_dir = storage/
